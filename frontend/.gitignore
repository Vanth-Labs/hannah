# Dependencias de Node
node_modules/
.pnpm-store/

# Compilación / Producción
dist/
build/

# Variables de entorno locales
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Sistema operativo
.DS_Store
Thumbs.db

# Assets excluidos del repo (comentarios en línea aparte: gitignore no los soporta inline)
# avatar de debug SMPL-X (19MB, no se usa en prod)
public/smplx_avatar.glb
# clips Mixamo crudos (licencia Adobe) — re-hornear con scripts/bake_mixamo.mjs
public/animations/*.fbx
# Se CONSERVAN: public/avatar.glb (VRM necesario) y public/animations/baked/*.json
public/vad/
